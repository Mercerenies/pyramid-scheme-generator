
package com.mercerenies.pyramids

import scala.language.implicitConversions

object ProjectEuler183 {
  import Code.*

  // Variables we use
  val sum = "sum"
  val x = "x"
  val n = "n"
  val y = "y"
  val recip = "recip"
  val k = "k"

  def code: Triangle =
    begin(
      set(sum, 0)
    )
}
